apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-flask-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eks-flask-test # Add this label
  template:
    metadata:
      labels:
        app: eks-flask-test
    spec:
      containers:
      - name: eks-flask-test
        image: {{.Values.image.repository }}:{{ .Values.image.tag }}
        ports:
        - containerPort: 8080
